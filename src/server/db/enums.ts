// Enums for the database

export enum RestaurantStatus {
  Active = "active",
  Inactive = "inactive",
  Disabled = "disabled",
}

export enum ToastVariant {
  Default = "default",
  Success = "success",
  Destructive = "destructive",
}
